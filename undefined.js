const undefinedInfo = 'Значение глобального свойства undefined представляет значение undefined. Это одно из примитивных значений JavaScript.'

    let result = null;
        if (result === null) {
            console.log('Результат не найден');
        } else {
            console.log('Результат: ' + result);    
        }

    const status = null;
        if (status === null) {
            console.log('Статус не определен');
        } else {
        console.log('Статус: ' + status);
        }